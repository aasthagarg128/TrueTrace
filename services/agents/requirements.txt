fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.2.3
httpx==0.28.1
numpy==1.26.4
opencv-python-headless==4.10.0.84
# Deliberately a floor, not a pin. Platform extractors break constantly and
# yt-dlp ships fixes within days; a hard pin silently rots. A 2025 pin was
# already failing against YouTube with "The page needs to be reloaded".
yt-dlp>=2026.8.19
cryptography==44.0.0
# Gemini, for plain-language explanations. Free tier via Google AI Studio.
# 0.5.0 predates the stable API and has no HttpOptions; it silently fell back
# to template text on every call. Floor, not pin: the model catalogue moves.
google-genai>=2.22.0
# Firestore, the optional cloud case backend. Free on Firebase's Spark plan.
google-cloud-firestore==2.20.0
# Cloud Storage and Pub/Sub were declared here but never imported. Both also
# require a billing account, so they are not part of the free-tier build.
# Re-add them alongside the code that actually uses them.
