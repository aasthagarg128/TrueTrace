fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.0
numpy==1.26.4
opencv-python-headless==4.10.0.84
mediapipe==0.10.18
transformers==4.47.1
# Capped by facenet-pytorch (identity.py), which requires Pillow <10.3.0.
# Only surfaced in a clean container build: locally facenet-pytorch was
# installed with --no-deps, which silently skipped this check.
pillow==10.2.0
# CPU-only torch. On Linux/Docker install with:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch==2.5.1
# Identity matching (app/identity.py). torchvision must match the torch pin
# above; install both from the same CPU index:
#   pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cpu
torchvision==0.20.1
facenet-pytorch==2.6.0
